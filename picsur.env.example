# Copy to `picsur.env` to enable the Share button (upload to a Picsur
# instance, copy the link to clipboard). Read at BUILD time by build.rs and
# baked in; rebuild after editing. Absent => Share disabled.

base_url=https://picsur.org
api_key=your-picsur-api-key
